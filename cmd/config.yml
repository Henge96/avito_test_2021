# Server
server:
  host: 0.0.0.0
  port:
    http: 8080
  timeout:
    server_timeout: 30
# Database
db:
    driver: postgres
    user: postgres
    hostdb: postgres
    portdb: 5432
    dbname: postgres
    mode: disable
# Clients
client:
  rest_api_base_path: https://api.apilayer.com/exchangerates_data